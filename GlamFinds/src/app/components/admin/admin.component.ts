import { Component } from '@angular/core';

// Componente de la sección "Admin".
// Actualmente es solo una plantilla vacía (placeholder): no tiene lógica ni
// variables propias. Su HTML solo muestra el menú superior (app-menu3) y una
// sección de contenido vacía. Sirve como base para un futuro panel de
// administración que todavía no fue implementado.
@Component({
  selector: 'app-admin',
  templateUrl: './admin.component.html',
  styleUrls: ['./admin.component.scss']
})
export class AdminComponent {

}
